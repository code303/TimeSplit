type Task = {
    started: number;
    stopped: number;
    projectName: string;
    description: string;
};

const TASK: Task = {
    started: 0,
    stopped: 0,
    projectName: null,
    description: 'taskDesc'
};